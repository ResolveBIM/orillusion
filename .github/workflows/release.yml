name: Legacy Release (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "This workflow is disabled. Release job moved to pack-and-test-web-nextjs.yml"

